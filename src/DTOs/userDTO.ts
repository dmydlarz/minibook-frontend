export type userDTO = {
  firstName: string;
  lastName: string;
  displayName: string;
  email: string;
  password: string;
  // confirmPassword: string;
};

export type loginDTO = {
  email: string;
  password: string;
};
